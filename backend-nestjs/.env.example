# Copia este archivo a .env antes de levantar el backend:
# cp .env.example .env

DATABASE_URL="postgresql://maraplus:maraplus_dev@localhost:5433/maraplus?schema=public"
# En local puede ser igual que DATABASE_URL. En Neon usa la URL "direct" (sin pooler).
DIRECT_URL="postgresql://maraplus:maraplus_dev@localhost:5433/maraplus?schema=public"
PORT=3010

# URL pública del API (producción). Ej: https://api.farmaexpress.com
PUBLIC_API_URL="http://127.0.0.1:3010"

JWT_SECRET="farmaexpress-dev-secret-cambiar-en-produccion"
JWT_EXPIRES_IN="7d"

ADMIN_EMAIL="admin@farmaexpress.com"
ADMIN_PASSWORD="Admin123!"

# Expressia / chat. Clave en https://aistudio.google.com/apikey
GEMINI_API_KEY=""
# Escáner de recetas (IA separada). Si no se define, usa GEMINI_API_KEY.
GEMINI_SCAN_API_KEY=""
# Modelo chat
GEMINI_MODEL="gemini-2.0-flash"
# Modelo escáner de recetas (separado)
GEMINI_SCAN_MODEL="gemini-2.0-flash-lite"

# Opcional: clave de RapidAPI para precios reales de Walmart en búsqueda de bebidas.
RAPIDAPI_KEY=""
